# Figure 4 — Nutrient concentration modulates invasion resistance and coalescence outcomes

```bash
python figures/fig4/make_panels.py
```

## Panels

| Panel | Content | Status |
|---|---|---|
| a | Schematic of the nutrient manipulation | Illustration, not generated from data |
| b | Failed-invasion frequency across media | Not yet ported — from pairwise invasion assay counts |
| c | Coalescence outcomes in similarity space, per medium | Generated by `make_panels.py` |
| d | Outcome fractions across the nutrient gradient | Generated by `make_panels.py` |

## Inputs

- `data/processed_CoalescenceEvent_synthetic.xlsx` — event metadata and parent
  sample IDs
- `data/processed_Sequences_synthetic.xlsx` — composition vectors

## Panel c: what the grey points mean

Every event is plotted twice: once under the original parental labelling
(coloured) and once with the two parental labels swapped (grey). The similarity
map has no intrinsic ordering of the two parents — calling one of them "parent
A" is arbitrary — so showing both makes that symmetry explicit instead of
implying a preferred axis. The same convention applies to the marginal
histograms and to Fig. 6b.

## Panel d: reproduction

`make_panels.py` prints the fractions it plots. **Exact.**

| | Published | This code |
|---|---|---|
| Dominance (Nutr−/Base/Nutr+) | 39% / 65% / 76% | 38.9% / 65.1% / 75.6% |
| Mixture (Nutr−/Base/Nutr+) | 53% / 4% / 6% | 53.3% / 3.6% / 5.6% |
| *n* (Nutr−/Base/Nutr+) | 90 / 83 / 90 | 90 / 83 / 90 |

This requires the quality-control exclusions in `coalescence.io.EXCLUDED_SAMPLES`
— 22 wells whose culture or sequencing failed, applied uniformly across every
figure in the study. They are on by default; pass
`outcome_table(apply_exclusions=False)` to see the unfiltered data, which gives
*n* = 92/88/94 and shifts the fractions by up to 3 points.

## Source

Ported from `Main_Fig_4/Generate_Fig4_1.ipynb` and the vector-decomposition
notebook `Main_Fig_1/Impt_Generate_Fig1_7_vectordecomposition.ipynb`, whose
`metric_VectorDecomposition_onlyPositive`, `calculate_assymetricity` and
`characterize_case` functions are now `coalescence.decomposition`.
