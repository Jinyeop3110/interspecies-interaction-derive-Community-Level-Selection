# Figure 1 — Coalescence of synthetic communities frequently yields Dominance

```bash
python figures/fig1/make_panels.py
```

## Panels

The published figure has panels a–e.

| Panel | Content | Status |
|---|---|---|
| a | Schematic of coalescence and the three outcome classes | Illustration |
| b | The two-parent similarity map and the similarity metric | Illustration |
| c | Experimental workflow | Illustration |
| d | Time courses of community composition across cycles 0–7 | **Not generated** — see below |
| e | Outcomes in similarity space, plus the outcome-fraction bar | Generated by `make_panels.py` |

## Panel d is not covered

Panel d shows stacked relative abundance across the seven growth-dilution
cycles for three representative coalescence events — two Community-Level
Selection, one Restructuring — with the parental time courses feeding into the
coalesced one.

It requires time-resolved composition data. The archived sequence tables carry
only the final timepoint (`Timepoint == "F"` for every sample in
`Metadata.xlsx`), so the panel cannot be regenerated from the deposited data.
The per-cycle sequencing behind it is not part of the archive.

## Panel e: reproduction

Exact, with the quality-control exclusions applied (the default).

| | Published | This code |
|---|---|---|
| Base *n* | 83 | 83 |
| Dominance | 65% (n = 54) | 65.1% |
| Restructuring | 31% (n = 26) | 31.3% |
| Mixture | 4% (n = 3) | 3.6% |

Grey points are the same events with the two parental labels swapped; the map
has no intrinsic parent ordering.

Not yet matched to the published panel: the richness-coded marker shapes
(circles for 6-species pools, squares for 12, triangles for 24) described in
the caption are not encoded here — all events are drawn with one marker.

## Source

`Main_Fig_1/Generate_Fig1_1..1_6.ipynb` (composition and time-course panels) and
`Main_Fig_1/Impt_Generate_Fig1_7_vectordecomposition.ipynb` (similarity map).
